{#
/**
 * @file
 * WaaP Login - Email Verification HTML Template
 *
 * Variables:
 * - verification_url: Full URL to email verification endpoint
 * - user_name: User's display name or username
 * - wallet_address: Ethereum wallet address (formatted)
 * - site_name: Site name from Drupal config
 * - expiration_hours: Hours until verification link expires (24)
 */
#}
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ 'Verify your email address'|t }}</title>
    <style type="text/css">
      /* Reset styles */
      body,
      table,
      td,
      p,
      a,
      li,
      blockquote {
        -webkit-text-size-adjust: 100%;
        -ms-text-size-adjust: 100%;
      }
      table,
      td {
        mso-table-lspace: 0pt;
        mso-table-rspace: 0pt;
      }
      img {
        -ms-interpolation-mode: bicubic;
        border: 0;
        height: auto;
        line-height: 100%;
        outline: none;
        text-decoration: none;
      }
      body {
        margin: 0 !important;
        padding: 0 !important;
        width: 100% !important;
        background-color: #f4f4f4;
      }
      a {
        color: #0066cc;
        text-decoration: underline;
      }
      /* Button styles */
      .button {
        display: inline-block;
        padding: 14px 32px;
        background-color: #0066cc;
        color: #ffffff !important;
        text-decoration: none !important;
        font-weight: bold;
        font-size: 16px;
        border-radius: 4px;
        -webkit-border-radius: 4px;
        -moz-border-radius: 4px;
      }
      .button:hover {
        background-color: #0055a3;
      }
      /* Responsive styles */
      @media only screen and (max-width: 600px) {
        .email-container {
          width: 100% !important;
        }
        .email-content {
          padding: 20px !important;
        }
        .button {
          width: 100% !important;
          box-sizing: border-box;
        }
      }
    </style>
  </head>
  <body style="margin: 0; padding: 0; font-family: Arial, Helvetica, sans-serif; background-color: #f4f4f4; line-height: 1.6; color: #333333;">
    <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background-color: #f4f4f4;">
      <tr>
        <td align="center" style="padding: 40px 10px;">
          <table role="presentation" class="email-container" width="600" cellpadding="0" cellspacing="0" style="background-color: #ffffff; border-radius: 8px; -webkit-border-radius: 8px; -moz-border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);">
            <!-- Header -->
            <tr>
              <td style="padding: 30px 30px 20px; border-bottom: 1px solid #e0e0e0;">
                <table role="presentation" width="100%" cellpadding="0" cellspacing="0">
                  <tr>
                    <td style="text-align: center;">
                      <h1 style="margin: 0; color: #0066cc; font-size: 28px; font-weight: bold;">{{ 'WaaP Authentication'|t }}</h1>
                    </td>
                  </tr>
                </table>
              </td>
            </tr>
            <!-- Content -->
            <tr>
              <td class="email-content" style="padding: 40px 30px;">
                <h2 style="margin: 0 0 20px; color: #333333; font-size: 24px;">{{ 'Verify Your Email Address'|t }}</h2>
                <p style="margin: 0 0 20px; color: #666666; font-size: 14px;">{{ 'Hello %user_name,'|t({ '%user_name': user_name }) }}</p>
                <p style="margin: 0 0 20px; color: #666666; font-size: 14px;">{{ 'Thank you for signing up with WaaP (Wallet as a Protocol) on %site_name. Please verify your email address to complete your account setup.'|t({ '%site_name': site_name }) }}</p>
                <p style="margin: 0 0 10px; color: #666666; font-size: 14px;">
                  <strong>{{ 'Wallet Address:'|t }}</strong>
                </p>
                <p style="margin: 0 0 30px; color: #333333; font-size: 14px; font-family: 'Courier New', monospace; background-color: #f9f9f9; padding: 12px; border-radius: 4px; border: 1px solid #e0e0e0;">{{ wallet_address }}</p>
                <!-- CTA Button -->
                <table role="presentation" cellpadding="0" cellspacing="0" style="margin: 0 auto;">
                  <tr>
                    <td style="text-align: center;">
                      <a href="{{ verification_url }}" class="button" target="_blank">{{ 'Verify Email Address'|t }}</a>
                    </td>
                  </tr>
                </table>
                <p style="margin: 30px 0 10px; color: #666666; font-size: 14px;">{{ "If the button above doesn't work, you can copy and paste the following link into your browser:"|t }}</p>
                <p style="margin: 0 0 30px; color: #666666; font-size: 12px; word-break: break-all;">
                  <a href="{{ verification_url }}" style="color: #0066cc; word-break: break-all;">{{ verification_url }}</a>
                </p>
                <hr style="border: none; border-top: 1px solid #e0e0e0; margin: 30px 0;" />
                <p style="margin: 0 0 10px; color: #999999; font-size: 12px;">{{ 'This verification link will expire in %hours hours.'|t({ '%hours': expiration_hours }) }}</p>
                <p style="margin: 0; color: #999999; font-size: 12px;">{{ 'If you did not request this verification, please ignore this email. Your account will not be created.'|t }}</p>
              </td>
            </tr>
            <!-- Footer -->
            <tr>
              <td style="padding: 20px 30px; background-color: #f9f9f9; border-top: 1px solid #e0e0e0; border-radius: 0 0 8px 8px; -webkit-border-radius: 0 0 8px 8px; -moz-border-radius: 0 0 8px 8px;">
                <p style="margin: 0; color: #999999; font-size: 12px; text-align: center;">&copy; {{ 'now'|date('Y') }} {{ site_name }} | {{ 'Powered by WaaP'|t }}</p>
              </td>
            </tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html>
